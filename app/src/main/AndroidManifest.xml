<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sudoajay.namaz_alert">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />



    <application
        android:name=".ui.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".ui.mainActivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".ui.BaseActivity" />
        <activity android:name=".ui.feedbackAndHelp.SendFeedbackAndHelp"/>
        <activity
            android:name=".ui.setting.SettingsActivity"
            android:label="@string/setting_text"
            android:theme="@style/SettingAppTheme" />
        <meta-data android:name="com.google.firebase.messaging.default_notification_icon"

            android:resource="@drawable/app_icon" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
      <receiver android:name=".ui.notification.NotificationCancelReceiver"
          android:exported="true"
          android:permission="com.sudoajay.namaz_alert.PERMISSION">
        <intent-filter android:priority="999" >
            <action android:name="com.sudoajay.namaz_alert.Action" />
        </intent-filter>
    </receiver>

    </application>

</manifest>